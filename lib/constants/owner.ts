/**
 * Label for the "owner-managed" verified badge shown on a claimed+approved venue page.
 * A config constant so it can be reworded without a rebuild (alternatives the founder may
 * pick: "Verified owner" / "Managed by the venue"). The badge appears as soon as a venue
 * is claimed (free) — it is NOT gated to the Pro tier.
 */
export const OWNER_BADGE_LABEL = "Owner-managed";
