/**
 * Versioned owner/venue Terms & Conditions (B3). Bump this when the /venue-terms copy
 * changes materially; acceptance is captured against this exact string at claim and at
 * first paid checkout, and a bump re-prompts (a prior version's acceptance no longer
 * counts as "current").
 */
export const TERMS_VERSION = "venue-1.0-2026-08";
